# Test Fixtures

This directory contains short audio samples and model files for integration testing.

- `sample.wav`: 5s stereo audio.
- `mock_model.onnx`: Dummy ONNX model that returns identity or zeros.
